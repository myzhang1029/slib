#include <slib.h>

int main(void)
{
    char *a = "we";
    int b = 12;
    printf("test-itoa: Original vaule: variable (char*)a with vaule "
           "%s\n\tvariable(int)b with vaule %d",
           a, b);
    printf("test-itoa: Call itoa(b, a, 10)");
    printf("test-itoa: Now b = %d, a = %s", b, d);
    return 0;
}
